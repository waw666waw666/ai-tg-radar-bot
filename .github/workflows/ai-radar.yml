name: AI Feishu Radar Bot

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/ai-radar.yml"
      - "main.py"
      - "requirements.txt"

  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: ai-feishu-radar-bot
  cancel-in-progress: true

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Show trigger info
        run: |
          echo "event_name=${{ github.event_name }}"
          echo "ref=${{ github.ref }}"
          echo "repository=${{ github.repository }}"
          echo "workflow=${{ github.workflow }}"
          echo "sha=${{ github.sha }}"
          date -u

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show workflow file
        run: |
          echo "===== .github/workflows/ai-radar.yml ====="
          sed -n '1,220p' .github/workflows/ai-radar.yml
          echo "===== end ====="

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip

          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

          pip install requests feedparser openai

      - name: Check secrets
        env:
          AGNES_API_KEY: ${{ secrets.AGNES_API_KEY }}
          AGNES_BASE_URL: ${{ secrets.AGNES_BASE_URL }}
          AGNES_MODEL: ${{ secrets.AGNES_MODEL }}
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        run: |
          python - <<'PY'
          import os

          print("AGNES_API_KEY set:", bool(os.environ.get("AGNES_API_KEY")))
          print("AGNES_BASE_URL set:", bool(os.environ.get("AGNES_BASE_URL")))
          print("AGNES_MODEL set:", bool(os.environ.get("AGNES_MODEL")))
          print("FEISHU_WEBHOOK set:", bool(os.environ.get("FEISHU_WEBHOOK")))
          print("FEISHU_WEBHOOK length:", len(os.environ.get("FEISHU_WEBHOOK", "")))

          print("MODELSCOPE_DISABLED: True")
          print("OFFICIAL_DEEPSEEK_DISABLED: True")
          PY

      - name: Run bot
        env:
          AGNES_API_KEY: ${{ secrets.AGNES_API_KEY }}
          AGNES_BASE_URL: ${{ secrets.AGNES_BASE_URL }}
          AGNES_MODEL: ${{ secrets.AGNES_MODEL }}
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
        run: python main.py

      - name: Commit seen cache
        if: github.event_name != 'push'
        run: |
          git config user.name "ai-radar-bot"
          git config user.email "ai-radar-bot@users.noreply.github.com"

          git add seen.json || true
          git commit -m "Update seen cache" || echo "No changes to commit"
          git push || true
